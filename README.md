# unity-mcp
Unity MCP Bridge connects the Unity Editor to any Model Context Protocol client — Claude Code, Codex, Cursor, Antigravity, or anything else that speaks MCP — so an AI coding agent can directly create GameObjects, write scripts, configure physics, build UI, manage assets, and construct Behavior Trees inside a real Unity project.
